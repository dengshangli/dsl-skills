interface:
  display_name: "Figma Overlay Check"
  short_description: "Add a removable single-file Figma overlay"
  default_prompt: "Use $figma-overlay-check to add the single-file overlay, actively fix the UI differences it reveals, verify the result, and then leave it for my confirmation and cleanup."
