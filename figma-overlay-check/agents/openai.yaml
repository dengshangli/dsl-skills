interface:
  display_name: "Figma Overlay Check"
  short_description: "Add a code-backed Figma comparison overlay"
  default_prompt: "Use $figma-overlay-check to add a persistent overlay in this project's code, compare the page with its Figma design, and leave it mounted until cleanup."
